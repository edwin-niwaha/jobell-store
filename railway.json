{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "DJANGO_ENV=production python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "DJANGO_ENV=production gunicorn core.wsgi"
  }
}
